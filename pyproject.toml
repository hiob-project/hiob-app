[project]
name = "hiob-app"
version = "0.1.0"
description = "Web application for the HIOB project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "acdh-cfts-pyutils>=0.2",
    "requests>=2.32.5",
    "tqdm>=4.68.3",
]
